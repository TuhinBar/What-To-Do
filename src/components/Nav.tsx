import classes from './Nav.module.css';

const Nav: React.FC = () => {
    return (
        <nav className={classes.nav}>
            <div>What To Do!</div>
        </nav>
    )
};

export default Nav;